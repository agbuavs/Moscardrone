int checkCommLoss(double nseq) {
//Operates with sequence number of received msg in order to check if communication is lost
  
  
  return(0);
}

int enterSafetyMode(int danger) {
//Depending on danger value, activates one of the safety flight mode: return to home, landing...
  
  return(0);
}
